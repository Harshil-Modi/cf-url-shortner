{
  "private": true,
  "name": "url-shortner",
  "version": "1.0.0",
  "description": "A service for customizing shared URLs",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Harshil Modi",
  "license": "MIT"
}
